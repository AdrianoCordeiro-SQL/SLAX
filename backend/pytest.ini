[pytest]
testpaths = tests
pythonpath = .
filterwarnings =
    ignore::DeprecationWarning:passlib
    ignore::jwt.warnings.InsecureKeyLengthWarning
addopts =
    -ra
    --strict-markers
    --tb=short
    -q

markers =
    integration: HTTP / full-stack tests via TestClient
