DATABASE_URL=postgresql://user:password@db:5432/portfolio_db
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=portfolio_db
JWT_SECRET=logsnag-seu-segredo-super-secreto-indescobrivel
# Comma-separated browser origins (required for CORS with credentials in production).
ALLOWED_ORIGINS=http://localhost:3000,https://your-app.vercel.app

# Opcional: credenciais usadas por `python -m app.seed` para criar a conta admin inicial.
SEED_ADMIN_EMAIL=admin@email.com
SEED_ADMIN_PASSWORD=admin123
